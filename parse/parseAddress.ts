import { Address } from "../core/address";

export function parseStrToAddress(rawAddress: string) : Address{
    //TODO
    return {
        rawAddress,
        addressLines: [],
        aliasMap: {}
    }
    
}